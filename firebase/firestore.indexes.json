{
  "//": "No composite indexes required. 'My squads' uses a single-field memberUids array-contains query and sorts by createdAt client-side, so the default single-field indexes suffice.",
  "indexes": [],
  "fieldOverrides": []
}
